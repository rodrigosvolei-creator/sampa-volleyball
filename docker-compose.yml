version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - sampa_data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  sampa_data:
